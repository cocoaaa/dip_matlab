function img = buildFromGPyr(pyr, expandScale)
% Reconstructs an image from the gaussian pyramid
% pyr: a cell containing images at multiple scales. level 1 contains the
% finest, maxLevel contains the coarsest.

end